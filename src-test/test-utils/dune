
(library
  (name ttag)
  (modules ttag)
  (preprocess (pps ppx_jane)))

(library
  (name metadata)
  (modules metadata)
  (libraries
    sexplib
    ttag)
  (preprocess (pps ppx_jane))
)

(executable
 (name feat_count)
 (modules feat_count)
 (libraries ttag latex_tbl metadata utils))